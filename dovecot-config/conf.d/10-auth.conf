# Authentication mechanisms
disable_plaintext_auth = no
auth_mechanisms = plain login

# Include SQL or other authentication backends as needed
passdb {
  driver = sql
  args = /etc/dovecot/dovecot-sql.conf.ext
}