# SMTP service
smtp      inet  n       -       y       -       -       smtpd

# Submission service (optional)
submission inet n       -       y       -       -       smtpd
  -o syslog_name=postfix/submission
  -o smtpd_tls_security_level=encrypt
  -o smtpd_sasl_auth_enable=yes

# Other services as needed
